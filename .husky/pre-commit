
# Lint and format staged files
pnpm lint-staged
lint_exit=$?

if [ $lint_exit -ne 0 ]; then
  exit $lint_exit
fi

# Type-check entire codebase
pnpm typecheck
typecheck_exit=$?

exit $typecheck_exit

